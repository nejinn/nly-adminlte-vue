/**
 * id
 * stats
 *
 * label-col-* xs sm md lg xl 给form-group div添加form-row类
 * 同时给label添加col-*-*或者col类 增加一个div 给默认包裹默认插槽，添加col-*-*或者col类
 *
 * is-valid 给给form-group div添加is-valid类
 *
 *
 *
 *
 * input
 */
