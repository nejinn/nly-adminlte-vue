const nlyId = function nlyId(id) {
  return `nly_id_${id}`;
};

const nlyDropdownParentId = function nlyDropdownParentId(id) {
  return `nly_dropdown_parent_id_${id}`;
};

const nlyDropdownId = function nlyDropdownId(id) {
  return `nly_dropdown_id_${id}`;
};

const nlyDropdownMenuId = function nlyDropdownMenuId(id) {
  return `nly_dropdown_menu_id_${id}`;
};

const nlyCollapseId = function nlyCollapseId(id) {
  return `nly_collapse_id_${id}`;
};

export {
  nlyId,
  nlyDropdownParentId,
  nlyDropdownId,
  nlyDropdownMenuId,
  nlyCollapseId
};
