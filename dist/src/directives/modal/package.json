{
  "name": "@bootstrap-vue/modal",
  "version": "0.0.0",
  "meta": {
    "title": "Modal",
    "subtitle": "模态框指令",
    "description": "可以绑定到可点击的元素上来显示和隐藏对应 id 的 modal",
    "directive": "VNlyModal",
    "arg": {
      "pattern": "[a-zA-Z][a-zA-Z0-9_\\-]*",
      "description": "需要展开和隐藏的 modal 的id",
      "required": true
    },
    "expression": [
      "String"
    ],
    "modifiers": [
      {
        "name": "{modalId}",
        "pattern": "[a-zA-Z][a-zA-Z0-9_\\-]*",
        "description": "需要打开的modal的id. 使用id 代替 `{modalId}`"
      }
    ]
  }
}
